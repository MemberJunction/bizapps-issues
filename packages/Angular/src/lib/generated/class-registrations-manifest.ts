/**
 * PLACEHOLDER — overwritten by `npm run mj:codegen` (manifest step).
 * Imports every @RegisterClass decorated form component by name into an exported
 * array, creating a static code path that prevents tree-shaking. Do not hand-edit.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const CLASS_REGISTRATIONS: any[] = [];

/** Marker constant indicating the manifest has been loaded. */
export const CLASS_REGISTRATIONS_MANIFEST_LOADED = true;
